Steps: 1. run 'make' to compile the code
       2. type './NaiveBayes ***.train ***.test' to run the code

Output: (train dataset) TP FN FP TN
	(test dataset)  TP FN FP TN

for more implementation details, you can read my report.
